CREATE TABLE "public"."offer"("limited" boolean NOT NULL, "online_only" boolean NOT NULL, "quantity" integer NOT NULL, "offer_type" varchar NOT NULL, "description" varchar NOT NULL, "start_date" varchar NOT NULL, "end_date" varchar NOT NULL, "images" varchar NOT NULL, "sponsor_id" integer NOT NULL, "id" serial NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("sponsor_id") REFERENCES "public"."user"("id") ON UPDATE no action ON DELETE no action);
